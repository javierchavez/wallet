


<!-- <div class="progress">
     <div class="progress-bar progress-bar-info" role="progressbar" aria-valuenow="20" aria-valuemin="0" aria-valuemax="100" style="width: 20%">
     <span class="sr-only">20% Complete</span>
     </div>
     </div>
   -->
<div class="row">
  <!-- <div class="col-xs-6"><%#%>= submit_tag 'Log a purchase', :type => 'button', :id => "spend-money", :class => "btn btn-default" %></div>; -->

  <!-- <div id="spend-form"> -->
    <%= render partial: "transactions/form_all", locals: { transaction: @transaction } %>  
  <!-- </div> -->
</div>

<div style="margin-top: 25px;"class="row">
  <p class="text-center"><button class="btn button-default" id="show-trans">Show last 10 transactions</button></p>
</div>

<div id="transactions-all" class="table-responsive">
  <table class="table table-hover">
    <caption>Transactions</caption>
    <thead>
    </thead>
    <tbody id="trans">
      <% @transactions.each do |trans| %>
        <tr>
          <td><%= link_to trans.name, edit_category_transaction_path(trans.category, trans) %></td>
          <td><em><small><%= number_to_currency(trans.amount, :precision => 2)  %></small></em></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>  

